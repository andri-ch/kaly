#:kivy 1.7.2

#<Kaly>:
#<Button>:
<FirstScreen>:
    canvas:
		Rectangle:
        #Image:
            #size_hint: None, None                     # important;
            size: self.size
            pos: self.pos
            # The default size_hint is (1, 1). If the parent is a Layout, then the widget size will be the parent/layout size.
            source: 'media/kaly-background5.png'

